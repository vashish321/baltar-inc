{
  "installCommand": "npm install && npm install --prefix apps/app",
  "buildCommand": "cd apps/app && npm run build",
  "outputDirectory": "apps/app/.next",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://baltar-inc-1.onrender.com/api/$1" }
  ]
}
