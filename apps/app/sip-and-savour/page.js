import PageContent from '../(hospitality)/hospitality/savour-and-sip/PageContent';

export const metadata = {
  title: 'Savour & Sip — Luxury Catering & Event Hospitality | Toronto, GTA',
  description: 'Savour & Sip provides luxury catering, curated bar programs, and full-service event hospitality across Toronto and the GTA.',
};

export default function SavourSipPage() {
  return <PageContent />;
}
